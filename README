# WCF - ServerOverview Plugin

This plugin adds a "Server Overview" page to an WCF (Woltlab Community Framework) driven site like the Woltlab Burning Board Lite.

## TODO:

- Add localization for languages other than german
- Configurable params, like replace colors etc.
- Server add/edit/delete in the backend
- Cache Serverinformation
